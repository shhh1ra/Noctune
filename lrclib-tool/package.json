{
  "name": "lrcget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "web:dev": "vite --host 127.0.0.1 --port 5180 --configLoader runner",
    "build:web": "vue-tsc --noEmit && vite build --configLoader runner",
    "build:portable": "tauri build --no-bundle"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vue-tsc": "^2.2.0"
  }
}
